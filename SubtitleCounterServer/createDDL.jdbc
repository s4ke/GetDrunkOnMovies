CREATE TABLE DBMOVIE (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE DRINKOCCASION (ID INTEGER NOT NULL, cnt INTEGER, TEXT VARCHAR(255), DBMOVIE_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE DRINKOCCASION ADD CONSTRAINT FK_DRINKOCCASION_DBMOVIE_ID FOREIGN KEY (DBMOVIE_ID) REFERENCES DBMOVIE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
